// const AppHelper = require('../../../src/components/AppHelper/AppHelper')

describe('The AppHelper', () => {
  // let helper = new AppHelper();

  it('', () => {
    // expect(imgBlocks.length).to.equal(24);
  });
});
